{% extends 'base.html.twig' %}

{% block title %}Edit CatTournois{% endblock %}

{% block body %}
    <!-- page start -->
    <div class="col-sm-8">
        <section class="panel">
            <div class="chat-room-head">
                <h3>
                    <i class="fa fa-angle-right"></i>
                    Editer une catégorie de tournois</h3>
            </div>
            <div class="panel-body">
                <h1>Edit Catégorie Tournois</h1>
                {{ include('cat_tournois/_form.html.twig', {'button_label': 'Enregistrer'}) }}
                <a href="{{path('app_cat_tournois_index')}}"></a>
                    <button class="btn btn-primary btn-xs">
                        <i class="fa fa-share"></i>
                    </button>
                </a>
                {{ include('cat_tournois/_delete_form.html.twig') }}
            </div> <!-- fin div panel-body-->
        </section> <!-- fin section catTournois-->
    </div> <!--fin div col-sm-8-->
{% endblock %}