{% extends 'base.html.twig' %}

{% block title %}CatTournois{% endblock %}

{% block body %}
    <h1>CatTournois</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_tournoi.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ cat_tournoi.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cat_tournois_index') }}">back to list</a>

    <a href="{{ path('app_cat_tournois_edit', {'id': cat_tournoi.id}) }}">edit</a>

    {{ include('cat_tournois/_delete_form.html.twig') }}
{% endblock %}
