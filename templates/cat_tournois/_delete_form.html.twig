<form method="post" action="{{ path('app_cat_tournois_delete', {'id': cat_tournoi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cat_tournoi.id) }}">
    <button class="btn">Delete</button>
</form>
