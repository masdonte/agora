<form method="post" action="{{ path('app_plateforme_delete', {'id': plateforme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plateforme.id) }}">
    <button class="btn btn-primary btn-xs"><i class=" fa fa-trash-o"></i></button>
</form>
