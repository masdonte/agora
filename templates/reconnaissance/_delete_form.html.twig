<form method="post" action="{{ path('app_reconnaissance_delete', {'id': reconnaissance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reconnaissance.id) }}">
    <button class="btn">Delete</button>
</form>
