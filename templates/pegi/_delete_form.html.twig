<form method="post" action="{{ path('app_pegi_delete', {'id': pegi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pegi.id) }}">
    <button class="btn btn-primary btn-xs"><i class=" fa fa-trash-o"></i></button>
</form>
